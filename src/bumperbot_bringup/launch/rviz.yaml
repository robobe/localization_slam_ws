launch:
  - arg:
      name: "rviz_config_file"
      default: "$(find-pkg-share bumperbot_bringup)/config/localization.rviz"

  - node:
      pkg: rviz2
      exec: rviz2
      name: rviz2
      output: screen
      args:
          "-d $(var rviz_config_file)"